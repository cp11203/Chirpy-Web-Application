<!DOCTYPE html>
<!-- All CSS code has been generated by chatpgt version 3.5, from OpenAI -->
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Follower</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 400px;
            text-align: center;
        }
        h1 {
            margin-top: 0;
        }
        label {
            font-weight: bold;
        }
        input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }
        button {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #0056b3;
        }
        .return-link {
            margin-top: 15px;
            font-size: 14px;
            color: #007bff;
            text-decoration: none;
        }
        .return-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Add Follower</h1>
        <form action="/main/addfollower/" method="post">
            <div>
                <label for="userToFollow">Username to Follow:</label>
                <input type="text" id="userToFollow" name="userToFollow" required>
            </div>
            <button type="submit">Add Follower</button>
        </form>
        
        <#if successMessage??>
            <p style="color: green;">${successMessage}</p>
        </#if>
        <#if errorMessage??>
            <p style="color: red;">${errorMessage}</p>
        </#if>

        <a href="/main/" class="return-link">Return to Main Page</a>
    </div>
</body>
</html>